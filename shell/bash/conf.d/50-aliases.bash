# Aliases and functions.
#
# Entirely shared with zsh, including the nine functions. The only thing zsh
# keeps to itself is zmv/mmv, which bash has no equivalent for.
. "$DOTFILES_SHELL/shared/50-aliases.sh"
